on init
    # Goodix fingerprint
    chmod 0666 /dev/goodix_fp
    chown system system /dev/goodix_fp

on boot
    chmod 0666 /dev/input/event2

    chown system system /sys/class/drm/card0-DSI-1/disp_param
    chmod 0644 /sys/class/drm/card0-DSI-1/disp_param
    chown system system /sys/class/drm/card0-DSI-1/panel_info
    chmod 0644 /sys/class/drm/card0-DSI-1/panel_info
    chown system system /sys/class/drm/card0-DSI-1/fod_ui_ready
    chmod 0644 /sys/class/drm/card0-DSI-1/fod_ui_ready

    chown system system /sys/devices/virtual/touch/tp_dev/fod_status
    chmod 0644 /sys/devices/virtual/touch/tp_dev/fod_status
    write /sys/devices/virtual/touch/tp_dev/fod_status 1

on post-fs-data
    mkdir /data/vendor/goodix 0770 system system
    mkdir /data/vendor/goodix/gf_data/authenticate 0770 system system
    mkdir /data/vendor/goodix/gf_data/enroll 0770 system system
    chown system system /data/vendor

service vendor.fps_hal /vendor/bin/hw/android.hardware.biometrics.fingerprint@2.3-service.xiaomi_grus
    # "class hal" causes a race condition on some devices due to files created
    # in /data. As a workaround, postpone startup until later in boot once
    # /data is mounted.
    class late_start
    user system
    group system input uhid
